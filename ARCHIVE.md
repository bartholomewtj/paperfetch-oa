# Archive

Public tree is the OA papers CLI run-kit plus current tests.

`NEXT-SESSION.md` and `SKILL.md` moved to `archive/` (gitignored). Factory dirs (`adws/`, `specs/`, `requests/`, `app_docs/`) are gitignored so a future SSSF stamp stays local.

Do not add `archive/` or factory paths to git.
